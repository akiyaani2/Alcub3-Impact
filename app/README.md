# ALCUB3 Impact App

This is the public web application for ALCUB3 Impact.

It is the guest-first product surface for the water-intelligence venture and includes:

- `/` — Water Intelligence homepage
- `/pulse` — Water Pulse
- `/footprint` — AI water footprint
- `/observatory` — Labs surface
- `/methodology` — product-facing trust layer
- `/answers` — public product explanation
- `/partners` — partner-facing overview
- `/progress` — visible progress surface
- `/impact-api` — hosted API overview
- `/account` — progressive access / gating model

## Product Principles

- first value before login
- public methodology
- explicit maturity labels
- clear split between Product, Labs, and Research

## Development

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Build

```bash
npm run lint
npm run build
```

## Notes

- This app is part of the public `Alcub3-Impact` repo.
- It should reflect the public venture story, not internal fundraising language.
- Hosted and gated layers should be additive, not required for first use.
